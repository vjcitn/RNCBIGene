% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache_ops.R
\name{cache_by_taxon}
\alias{cache_by_taxon}
\title{filter and cache parquet resources for a single taxon in BiocFileCache}
\usage{
cache_by_taxon(
  taxid,
  resources = sub("\\\\.parquet$", "", available_ncbi_parquet()),
  cache = .ncbi_cache(),
  force = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{taxid}{integer(1) NCBI taxonomy ID (e.g. 9606L for human, 10090L for mouse)}

\item{resources}{character vector of resource names (without .parquet suffix)
to cache; defaults to all resources in the bucket}

\item{cache}{a \code{BiocFileCache} object}

\item{force}{logical(1) if TRUE, re-download and overwrite existing cache entries}

\item{verbose}{logical(1) print progress messages}
}
\value{
invisibly, a named character vector of local parquet paths
}
\description{
Performs a one-time download-and-filter of remote NCBI Gene parquet files to
local parquet files stored in BiocFileCache.  After calling this function,
\code{open_ncbi_gene(resource, taxid)} will automatically route to the local
cached file instead of querying the OSN bucket, with no change to the API.
}
\examples{
if (is_online()) {
  # cache only the smallest resource for illustration
  cache_by_taxon(10090L, resources = "gene_orthologs")
  taxon_cache_info(10090L)
}
}
