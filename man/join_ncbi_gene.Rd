% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join_ncbi.R
\name{join_ncbi_gene}
\alias{join_ncbi_gene}
\title{join a local data frame to a remote NCBI Gene parquet resource}
\usage{
join_ncbi_gene(
  local_df,
  by,
  resource = "gene_info",
  taxid = NULL,
  type = "left"
)
}
\arguments{
\item{local_df}{data.frame to join against the remote resource}

\item{by}{character vector of column name(s) to join on; must exist in both
\code{local_df} and the remote resource}

\item{resource}{character(1) parquet resource name, with or without .parquet suffix;
must be one of \code{available_ncbi_parquet()}}

\item{taxid}{integer(1) or NULL; when non-NULL, pre-filters the remote resource to this taxonomy ID}

\item{type}{character(1) "left" (default) or "inner"}
}
\value{
lazy dplyr::tbl -- pipe select/filter/collect as needed
}
\description{
join a local data frame to a remote NCBI Gene parquet resource
}
\note{
local_df is written to the persistent duckdb session. The returned tbl is lazy;
  call collect() to retrieve results. Both sides of the join run entirely in duckdb.
}
\examples{
if (is_online()) {
  local_df <- data.frame(Symbol = c("ORMDL3","TP53","BRCA1"))
  join_ncbi_gene(local_df, by = "Symbol", taxid = 9606L) |>
    dplyr::select(Symbol, GeneID, map_location) |>
    dplyr::collect()
}
}
